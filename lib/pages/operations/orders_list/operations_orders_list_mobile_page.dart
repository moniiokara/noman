import 'package:flutter/material.dart';

class OperationsOrderListMobilePage extends StatelessWidget {
  static const String routeName = 'operationsOrderListMobilePage';
  const OperationsOrderListMobilePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
