import 'package:flutter/material.dart';

class ZonalManagerOrderListTabletPage extends StatelessWidget {
  static const String routeName = 'zonalManagerOrderListTabletPage';
  const ZonalManagerOrderListTabletPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
