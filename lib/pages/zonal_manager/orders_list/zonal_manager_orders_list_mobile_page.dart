import 'package:flutter/material.dart';

class ZonalManagerOrderListMobilePage extends StatelessWidget {
  static const String routeName = 'zonalManagerOrderListMobilePage';
  const ZonalManagerOrderListMobilePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
