import 'package:flutter/material.dart';

class ZonalManagerOrderListDesktopPage extends StatelessWidget {
  static const String routeName = 'zonalManagerOrderDetailsTabletPage';
  const ZonalManagerOrderListDesktopPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
