import 'package:flutter/material.dart';

class CreateDealerDesktopPage extends StatelessWidget {
  static const String routeName  = 'createDealerDesktopPage';
  const CreateDealerDesktopPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
