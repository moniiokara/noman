import 'package:flutter/material.dart';

class CreateDealerMobilePage extends StatelessWidget {
  static const String routeName = 'createDealerMobilePage';
  const CreateDealerMobilePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
