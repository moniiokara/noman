import 'package:flutter/material.dart';

class CreateDealerTabletPage extends StatelessWidget {
  static const String routeName = 'createDealerTabletPage';
  const CreateDealerTabletPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
