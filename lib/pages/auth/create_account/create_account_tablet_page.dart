import 'package:flutter/material.dart';

class CreateAccountTabletPage extends StatelessWidget {
  static const String routeName = 'createAccountTabletPage';
  const CreateAccountTabletPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
