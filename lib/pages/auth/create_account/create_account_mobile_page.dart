import 'package:flutter/material.dart';

class CreateAccountMobilePage extends StatelessWidget {
  static const String routeName = 'createAccountMobilePage';
  const CreateAccountMobilePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
