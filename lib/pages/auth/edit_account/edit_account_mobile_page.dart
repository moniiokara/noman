import 'package:flutter/material.dart';

class EditAccountMobilePage extends StatelessWidget {
  static const String routeName = 'editAccountMobilePage';
  const EditAccountMobilePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
