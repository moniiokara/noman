import 'package:flutter/material.dart';

class EditAccountDesktopPage extends StatelessWidget {
  static const String routeName = 'editAccountDesktopPage';
  const EditAccountDesktopPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
