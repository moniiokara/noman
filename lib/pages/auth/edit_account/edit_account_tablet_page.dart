import 'package:flutter/material.dart';

class EditAccountTabletPage extends StatelessWidget {
  static const String routeName = 'editAccountTabletPage';
  const EditAccountTabletPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
