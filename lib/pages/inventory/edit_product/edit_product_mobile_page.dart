import 'package:flutter/material.dart';

class EditProductMobilePage extends StatelessWidget {
  static const String routeName = 'editProductMobilePage';
  const EditProductMobilePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
