import 'package:flutter/material.dart';

class AddProductTabletPage extends StatelessWidget {
  static const String routeName = 'addProductTabletPage';
  const AddProductTabletPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
