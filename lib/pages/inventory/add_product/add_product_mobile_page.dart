import 'package:flutter/material.dart';

class AddProductMobilePage extends StatelessWidget {
  static const String routeName = 'addProductMobilePage';
  const AddProductMobilePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
