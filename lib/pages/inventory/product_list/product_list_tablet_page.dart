import 'package:flutter/material.dart';

class ProductListTabletPage extends StatelessWidget {
  static const String routeName = 'productListTabletPage';
  const ProductListTabletPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
