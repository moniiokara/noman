import 'package:flutter/material.dart';

class ProductListMobilePage extends StatelessWidget {
  static const String routeName = 'productListMobilePage';
  const ProductListMobilePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
