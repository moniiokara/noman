import 'package:flutter/material.dart';

class ProductListDesktopPage extends StatelessWidget {
  static const String routeName = 'productListDesktopPage';
  const ProductListDesktopPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
