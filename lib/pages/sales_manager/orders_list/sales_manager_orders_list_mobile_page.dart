import 'package:flutter/material.dart';

class SalesManagerOrderListMobilePage extends StatelessWidget {
  static const String routeName = 'salesManagerOrderListMobilePage';
  const SalesManagerOrderListMobilePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
