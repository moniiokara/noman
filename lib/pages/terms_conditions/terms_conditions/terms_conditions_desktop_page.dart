import 'package:flutter/material.dart';

class TermsConditionsDesktopPage extends StatelessWidget {
  static const String routeName = 'termsConditionsDesktopPage';
  const TermsConditionsDesktopPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
