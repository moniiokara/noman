import 'package:flutter/material.dart';

class TermsConditionsMobilePage extends StatelessWidget {
  static const String routeName = 'termsConditionsDesktopPage';
  const TermsConditionsMobilePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
