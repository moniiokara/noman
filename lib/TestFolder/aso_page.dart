import 'package:flutter/material.dart';

class AsoPage extends StatelessWidget {
  const AsoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: Text("ASO"),
      ),
      body: Center(
        child: Text("aso"),
      ),
    );
  }
}
