import 'package:flutter/material.dart';

class DealerPage extends StatelessWidget {
  const DealerPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: Text("DealerPage"),
      ),
      body: Center(
        child: Text("DealerPage"),
      ),
    );
  }
}
